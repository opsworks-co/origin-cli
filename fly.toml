app = "origin-platform"
primary_region = "iad"

[build]
  dockerfile = "Dockerfile"
  [build.args]
    VITE_API_URL = "https://origin-platform.fly.dev"

[env]
  PORT = "8080"
  NODE_ENV = "production"
  JWT_SECRET = "change-me-in-production-secrets"

[http_service]
  internal_port = 8080
  force_https = true
  auto_stop_machines = true
  auto_start_machines = true

  [http_service.concurrency]
    type = "connections"
    hard_limit = 25
    soft_limit = 20

[[vm]]
  memory = "512mb"
  cpu_kind = "shared"
  cpus = 1

[mounts]
  source = "origin_data"
  destination = "/data"
