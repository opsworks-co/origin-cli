[
  "commands/enable.ts  const isGlobalBase = (basePath: string) => basePath === os.homedir();",
  "commands/enable.ts  const claudeLabel = gitRoot === os.homedir() ? `~/.claude/settings.json` : `.claude/settings.json`;",
  "commands/enable.ts  const cursorLabel = gitRoot === os.homedir() ? `~/.cursor/hooks.json` : `.cursor/hooks.json`;",
  "commands/enable.ts  const geminiLabel = gitRoot === os.homedir() ? `~/.gemini/settings.json` : `.gemini/settings.json`;",
  "commands/enable.ts  const devinLabel = gitRoot === os.homedir() ? `~/.devin/hooks.v1.json` : `.devin/hooks.v1.json`;",
  "commands/enable.ts  const label = gitRoot === os.homedir() ? `~/.windsurf/hooks.json` : `.windsurf/hooks.json`;",
  "commands/enable.ts  const isGlobal = gitRoot === os.homedir();",
  "commands/enable.ts  const codexLabel = gitRoot === os.homedir() ? `~/.codex/hooks.json` : `.codex/hooks.json`;",
  "commands/enable.ts  const isGlobalInstall = gitRoot === os.homedir();",
  "commands/hooks/user-prompt-submit.ts  if (s.repoPath !== opts.repoPath && s.repoPath !== opts.canonicalRepoPath) continue;",
  "commands/hooks.ts  const sessionsInRepoPath = hookCwd !== repoPath ? listActiveSessions(repoPath) : [];",
  "commands/hooks.ts  || (repoPath !== hookCwd ? findSessionByClaudeId(claudeSessionId, repoPath) : null);",
  "commands/hooks.ts  if (sessions.length === 0 && repoPath !== hookCwd) {",
  "commands/hooks.ts  const legacy = loadSessionState(hookCwd) || (repoPath !== hookCwd ? loadSessionState(repoPath) : null);",
  "commands/hooks/session-start.ts  const repoConfig = loadRepoConfig(canonicalRepoPath) || (canonicalRepoPath !== repoPath ? loadRepoConfig(repoPath) : null);",
  "commands/hooks/session-start.ts  if (repoPath !== hookCwd) clearSessionState(hookCwd, stale.sessionTag);",
  "commands/hooks/session-start.ts  if (repoPath !== hookCwd) clearSessionState(hookCwd, s.sessionTag);",
  "commands/hooks/session-start.ts  if (s.repoPath !== repoPath) continue;",
  "commands/hooks/session-start.ts  if (repoPath !== hookCwd) clearSessionState(repoPath);",
  "commands/hooks/user-prompt-submit.ts  } else if (state.transcriptPath !== incomingTranscriptPath) {",
  "commands/hooks/user-prompt-submit.ts  const repoConfig = loadRepoConfig(canonicalRepoPath || repoPath) || (canonicalRepoPath !== repoPath ? loadRepoConfig(repoPath) : null);",
  "commands/hooks/stop.ts  if (s.repoPath !== recoveryRepoPath) continue;",
  "commands/hooks/stop.ts  if (actualCwd && actualCwd !== state.repoPath && fs.existsSync(actualCwd)) {",
  "commands/hooks/tool-use.ts  if (hookCwd && state.lastCwd !== hookCwd) {",
  "commands/hooks.ts  if (localSettingsPath !== userSettingsPath && fs.existsSync(localSettingsPath)) {",
  "commands/hooks.ts  if (localPath !== userHooksPath && fs.existsSync(localPath)) {",
  "enabled-repos.ts  if (!repoPath || repoPath === os.homedir()) return;",
  "enabled-repos.ts  const entries = readEntries().filter((e) => e.path !== repoPath);",
  "enabled-repos.ts  const next = entries.filter((e) => e.path !== repoPath);",
  "git-capture.ts  if (fromTree === toTree) return empty;",
  "git-capture.ts  if (baseTree === commitTree) return { diff: '', linesAdded: 0, linesRemoved: 0 };",
  "local-db.ts  if (opts?.repoPath && p.repoPath !== opts.repoPath) return false;",
  "session-state.ts  if (mainRepo && mainRepo !== workRoot && fs.existsSync(mainRepo)) {",
  "session-state.ts  for (const p of (fbPath !== statePath ? [statePath, fbPath] : [statePath])) {",
  "session-state.ts  if (fbPath !== statePath) { try { fs.unlinkSync(fbPath); } catch { /* ignore */ } }",
  "todo.ts  items = items.filter(i => i.repoPath === repoPath);",
  "utils/sqlite.ts  if (!dbCache || dbCache.path !== dbPath || dbCache.mtimeMs !== mtimeMs) {"
]
